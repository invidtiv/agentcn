---
title: CSV to Questions
description: Summarizes a CSV dataset to stay within token limits, then generates focused analytical questions.
---

<DocsBaseSwitcher base="dawn" agent="csv-to-questions" />

<AgentPreview
  agent="csv-to-questions"
  framework="dawn"
  inputFields={[
    {
      name: "url",
      label: "CSV URL",
      placeholder: "https://example.com/world-gdp.csv",
      type: "text",
    },
  ]}
/>

## Summary

The **CSV to Questions Agent** takes a CSV dataset and turns it into a set of
sharp, answerable analytical questions. It first summarizes the data — columns,
types, ranges, patterns — to compress large files and avoid token-limit errors,
then generates questions a data analyst would actually ask. Reach for it to
kickstart exploratory analysis or build study material from raw data.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/dawn/csv-to-questions
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @dawn-ai/sdk
```

<Step>Copy and paste the following Dawn route files into your project.</Step>

<ComponentSource
  src="registry/dawn/csv-to-questions/index.ts"
  title="index.ts"
/>

<ComponentSource
  src="registry/dawn/csv-to-questions/instructions.md"
  title="instructions.md"
/>

<ComponentSource
  src="registry/dawn/csv-to-questions/tools/fetch_csv.ts"
  title="tools/fetch_csv.ts"
/>

<ComponentSource
  src="registry/dawn/csv-to-questions/tools/generate_questions.ts"
  title="tools/generate_questions.ts"
/>

<Step>
  Run `dawn check` after copying so Dawn regenerates route and tool metadata.
  `dawn dev`, `dawn build`, and `dawn verify` also run the same discovery path.
</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── index.ts
├── instructions.md
├── tools/fetch_csv.ts
├── tools/generate_questions.ts
```

## Customization

- Edit `index.ts` to change the model, description, or system prompt.
- Add route-local tools under `tools/`; Dawn infers tool input and output types from TypeScript.
- Run `dawn verify` before deploying generated LangGraph artifacts.
