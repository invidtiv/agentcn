---
title: Google Sheets Analysis
description: Reads, analyzes, and edits Google Sheets via the Sheets API.
---

<DocsBaseSwitcher base="langraph" agent="google-sheets" />

<AgentPreview
  agent="google-sheets"
  framework="langraph"
  inputFields={[
    {
      name: "spreadsheetId",
      label: "Spreadsheet ID",
      placeholder: "1A2b3C4d5E6f7G8h9I0j…",
      type: "text",
    },
    {
      name: "request",
      label: "Request",
      placeholder: "Total revenue by region and write it to F1.",
      type: "text",
    },
  ]}
/>

## Summary

The **Google Sheets Analysis Agent** reads a spreadsheet, answers questions about
it, and — on request — writes results back. It loads ranges in A1 notation,
computes totals/trends/joins, and updates cells only when you ask. Reach for it to
turn a sheet into a conversational analyst or to automate routine spreadsheet
edits.

## Installation

<CodeTabs>
<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @agentcn/langraph/google-sheets
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @dawn-ai/sdk
```

<Step>Copy and paste the following agent route files into your project.</Step>

<ComponentSource
  src="registry/langraph/google-sheets/index.ts"
  title="index.ts"
/>

<ComponentSource
  src="registry/langraph/google-sheets/instructions.md"
  title="instructions.md"
/>

<ComponentSource
  src="registry/langraph/google-sheets/tools/read_range.ts"
  title="tools/read_range.ts"
/>

<ComponentSource
  src="registry/langraph/google-sheets/tools/update_range.ts"
  title="tools/update_range.ts"
/>

<Step>
  Run `dawn check` after copying so route and tool metadata are regenerated.
  `dawn dev`, `dawn build`, and `dawn verify` also run the same discovery path.
</Step>

</Steps>

</TabsContent>
</CodeTabs>

## Composition

```text
├── index.ts
├── instructions.md
├── tools/read_range.ts
├── tools/update_range.ts
```

## Customization

- Edit `index.ts` to change the model, description, or system prompt.
- Add route-local tools under `tools/`; tool input and output types are inferred from TypeScript.
- Run `dawn verify` before deploying generated LangGraph artifacts.
